// Vídeo de fundo da tela de login do portal do membro (culto/adoração).
// Deixe string vazia para usar apenas o fundo escuro em degradê.
//
// Para adicionar um vídeo de forma CONFIÁVEL (recomendado), coloque um arquivo
// .mp4 curto (loop, ~5-15s, sem áudio) em `web/public/portal-bg.mp4` e use:
//   export const PORTAL_BG_VIDEO = '/portal-bg.mp4';
// (Evite links de bancos de vídeo externos — costumam bloquear/expirar.)
export const PORTAL_BG_VIDEO = '';
